name: Lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  zsh-syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install zsh
        run: sudo apt-get update && sudo apt-get install -y zsh
      - name: Check zsh syntax
        run: zsh -n zsh-ollama-completion.plugin.zsh
